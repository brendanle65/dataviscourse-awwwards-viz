module.exports = {
  /* Print semicolons at the ends of statements. */
  semi: true,

  /* Use single quotes instead of double quotes. */
  singleQuote: true,

  /* No trailing commmas. */
  trailingComma: "none",

  /* Print spaces between brackets in object literals. */
  bracketSpacing: true,

  /* Do not include parentheses around a sole arrow function parameter. */
  arrowParens: "avoid",

  /* Specifies the line length that the printer will wrap on. */
  printWidth: 120,

  /* Specifies the number of spaces per indentation-level. */
  tabWidth: 2,

  /* Indent lines with spaces instead of tabs. */
  useTabs: false,

  /* Use carriage return and line Feed only, common on windows machines. */
  endOfLine: "crlf",

  /* Do not put the > of a multi-line JSX element at the end of the last line. */
  jsxBracketSameLine: false,

  /* Use single quotes instead of double quotes in JSX. */
  jsxSingleQuote: true,
};
